/**
 * Lexical Scope:
 *          When a function is created inside the another function
 *          the inner function can access variables from the outer function
 *          scope (and even further out to the global scope )
 * 
 * Closure: When a function is created inside the another function and
 *          allowing to inner function to access variables from the outer 
 *          function scope, even after the outer function has finished their execution.
 * 
 */