if(true)
{
    var a=10;
    let b='Hello WOrld!';
   // const c=true;
}
console.log(a);
//console.log(b);
//console.log(c);

// for comment  ctrl+/   shift+alt+a

/* 1. var var has a global scope and function scope
           we reinitialize in same scope as well as 
           we can redeclr with same name

2. let let has only block scope
3. const const has also block scope */